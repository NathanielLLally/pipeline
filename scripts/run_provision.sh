#!/bin/sh
TOP=$PWD
. $TOP/.env

echo "TODO: spin up gcp vps instance, setup image/containers"
#ssh to vps host, run scraping worker process from docker container
#ssh -p $REST_SSH_PORT "$REST_SSH_USER@$REST_SSH_HOST" 
